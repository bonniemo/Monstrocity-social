import { StyleSheet } from "react-native";

export const flexStyles = StyleSheet.create({
  row: {
    flexDirection: "row",    
  },
  columnGap: {
    columnGap: 10,
  },
  columnGapL: {
    columnGap: 12,
  },
});
